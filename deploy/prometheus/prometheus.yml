global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "lsm-backend"
    authorization:
      type: Bearer
      credentials: "${METRICS_TOKEN:?set METRICS_TOKEN in the environment before starting Prometheus}"
    static_configs:
      - targets: ["backend:8080"]
